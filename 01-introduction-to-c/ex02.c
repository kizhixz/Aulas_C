
#include <stdio.h>

int main(void) {
    // A função printf() é usada para enviar saída formatada para o terminal.
    printf("Estou aprendendo a programar em C.\n");
    
    // Retorna 0 para indicar que o programa foi executado com sucesso.
    return 0;
}